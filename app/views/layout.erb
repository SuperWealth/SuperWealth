<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css">

<!DOCTYPE html>
<% title="Super Wealth" %>
<html lang="en">
<head>
  <title><%= @title || "SuperWealth" %></title>
  <meta charseet="utf-5">
  <link rel="stylesheet" href="/css/styles.css">
</head>
  <header>
    <h1><%= title %></h1>
    </header>
  <body>
 <% if flash[:errors] && !flash[:errors].empty? %>
   <div id="errors">
      <% flash[:errors].each do |error| %>
         <%= error %>
    </div>
      <% end %>
    </ul>
  <% end %>
  <% if flash[:notice] %>
    <div id="notice">
      <%= flash[:notice] %>
    </div>
  <% end %>
   <%= yield %>
</body>
</html>